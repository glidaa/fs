type Note @model @auth(rules: [{allow: private}]) {
  id: ID!
  note: String!
  isDone: Boolean!
  task: String
  description: String
  steps: String
  due: String
  assigned: String
  watcher: String
  project: String
  tag: [String]
  sprint: String
  status: String
  comments: [Comment] @connection(keyName: "byNote", fields: ["id"])
}

type Comment @model @key(name: "byNote", fields: ["noteID"]) @auth(rules: [{allow: private}]) {
  id: ID!
  noteID: ID!
  commenter: String
  content: String!
}
 